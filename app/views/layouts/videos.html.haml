!!! 5
%html{'ng-app' => 'lolCat'}
	%head
	%base{:href => '/'}
	%title LolCat
	= stylesheet_link_tag    'application', media: 'all'
	= javascript_include_tag 'application'
	= csrf_meta_tags
	%body
		%div{:id => "div-user-data", "data-user-id" => "#{user_signed_in? ? current_user.id: nil}" }
		= render "shared/header"
		.container-fluid
			.row
			= yield
			%div{'ng-view' =>''}